Drop the kiosk idle-screen background video here as `kiosk_background.mp4` (referenced by
`lib/Feactures/POS/Kiosk/kiosk_start_screen.dart`). Until a real file exists, the kiosk start
screen falls back to a plain branded gradient instead of a video.
